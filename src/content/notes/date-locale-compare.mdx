---
date: "2026-07-05"
tags: ["typescript", "snippet"]
draft: true
---

`YYYY-MM-DD` 문자열은 사전순이 곧 시간순이라 `Date` 객체로 바꿀 필요 없이 그냥 비교해도 된다.

```ts
notes.sort((a, b) => b.date.localeCompare(a.date)) // 최신순
```

포맷이 고정돼 있다는 전제만 지키면 파싱 비용 없이 정렬 끝.
